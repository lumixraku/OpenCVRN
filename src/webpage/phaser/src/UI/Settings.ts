import { EF_SCENE, SETTINGS_SCENE } from "@root/constants";

export class Settings extends Phaser.Scene {
        constructor() {
            super(SETTINGS_SCENE)
        }

        preload () {

        }


        create() {
            
        }
}